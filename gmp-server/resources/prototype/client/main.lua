local Anims = require('client.anims')
local Map = require('client.map')
local Watch = require('client.watch')
local VoiceChat = require('client.voice')
local Discord = require('client.discord')

exports = {
  Anims = Anims,
  Map = Map,
  Watch = Watch,
  VoiceChat = VoiceChat,
  Discord = Discord
}
