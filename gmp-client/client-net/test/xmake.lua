target("ClientNpcPacketTest")
    set_kind("binary")
    add_files("npc_packet_test.cpp")
    add_deps("Client.Net")
    add_packages("gtest")
    add_tests("default")
    set_rundir(os.projectdir())
    set_default(false)
    set_languages("c++20")
